{
    "version":2,
    "buildCommand": "npm run build",
    "builds": [
        {
        "src": "dist/",
        "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "dist/(.*)",
            "dest": "dist/"
        }
    ]
}